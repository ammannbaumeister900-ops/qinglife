# 轻生活运营管理后台

Vue 2 + Element UI 管理后台，覆盖轻友档案、期次、报名与付款、签到、改期、服务记录、工作人员配置以及系统权限管理。

```bash
npm install
npm run dev
npm run build:stage
```

测试环境使用 `.env.staging`；正式部署前必须核对 API 地址和回退包。不要提交账号、Cookie、日志或构建后的 `dist/`。
