# 中断恢复与生产配置 HTTP 验收

## 已执行证据

2026-09-21恢复工作时读取现有日志和Failsafe报告，确认上一轮于2026-09-17 06:57:45结束的 Maven `-Phttp-it -pl box-web -am verify` 为 BUILD SUCCESS。dev与prod各5项，共10项HTTP集成测试，0失败/错误/跳过；该轮21项单元测试通过。

ProductionLoginHttpIT继承同一登录契约并启用prod配置及配置/身份索引守卫，公共图片地址使用example.invalid占位，微信接口仍为测试替身。MySQL与Redis真实执行。数据库故障注入由需要SUPER权限的触发器改为隔离测试表上的CHECK约束，应用账号无需获得全局超级权限。

测试账号由QINGLIFE_HTTP_MYSQL_USER/PASSWORD环境变量指定；两个profile运行时应使用仅限独立测试库的临时账号及非空密码。原始日志保存本机忽略目录，不将包含环境属性的XML报告上传GitHub。

这不代表生产部署、真实微信真机、反向代理或对象存储验收。此报告明确是恢复时确认的9月17日执行证据，不称为9月21日重跑结果。

## 预览界面整合

2026-09-21将Terra提交fd2e378以f7b5242合入integration分支，保留轻友端工作人员入口及工作台5个UI文件差异，并保留每期独立统计修复。整合工作区重新执行node scripts/test-miniapp.cjs全部通过，生命周期10组仍为本地Mock HTTP链路，不是真实Java服务或微信真机。

## 用户确定的习惯计划规则

14/21天按有效自然日推进；暂停天数不计入；漏打卡不延长；到期结束。暂停当天不计入，恢复后从被冻结的计划日继续。暂停当天已有真实记录不能删除。实现尚未完成，不应将计划状态推进列为通过。
