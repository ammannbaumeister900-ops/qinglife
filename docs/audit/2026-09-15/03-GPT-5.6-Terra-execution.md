# Terra 整改执行记录

日期：2026-09-15。本文记录 `03-GPT-5.6-Terra.md` 的实际局部改动与仍受上游契约约束的事项；不是发布通过声明。

## 已实施

- **T-01**：`box-web` 的 `mysql-it` Failsafe 配置已显式设为 `${project.build.outputDirectory}`。Spring Boot 重打包 jar 不再作为集成测试的普通类目录。
- **T-02（机械适配）**：工作人员图片下载现在使用 `business-api.baseUrl()`，与 `business.login()` 取 token 时使用同一已解析源站。新增合成 `wx.downloadFile` 测试，覆盖 development、staging、production 三种现有解析分支。生产源站白名单及禁止运行时覆盖仍属于 S-03，尚未在此文档中假定完成。
- **T-03**：公开期次的日期改为一次批量查询后按期次回填；空列表不发 `IN ()`。工作人员访谈分页与最多 100 条访谈均改为一次图片批量查询后回填。返回字段、排序、分页上限和可见范围未改变。
- **T-04**：`apps/miniapp` 的 `test` 已改为复用根 `scripts/test-miniapp.cjs`，因此根与子包发现同一组测试。根 README 已区分 Java `clean package` 和隔离 MySQL 的 `-Pmysql-it verify`，说明 pnpm 锁文件策略与 `.env.example` 的非自动加载行为。
- **T-05**：已修正后台包名、说明、作者、关键词和模板仓库 URL。未删除 `src/views/demo` 等候选页面：菜单来自数据库动态路由，尚无不可达证明。

## 归档清点（T-07）

- `历史归档/2026-09-15/轻生活数据库升级V1.1/tests/auth-contract.test.mjs` 依赖历史 AppID 文件、旧后端路径和 `auth-decisions.json`，不能原样迁入。
- `registration-import.test.mjs` 使用 512 条脱敏夹具并验证导入归一化；它仍涉及历史导入策略，保留在归档，待 A-02 决定迁移契约后再适配。
- 归档 SQL 含 `INSERT`/`UPDATE` 菜单与迁移操作；没有复制到当前仓库或作为演示数据执行。归档原件保持不动。

## 依赖与构建核查（T-06）

| 范围 | 实际版本证据 | 当前动作 |
| --- | --- | --- |
| 管理后台 | `axios` 0.21.0、Vue 2.6.12；`pnpm-lock.yaml` 解析 Webpack 4.47.0 | 未升级。0.x 版本与旧构建链的兼容性/调用路径尚未逐项验证；升级会超出局部补丁范围。 |
| Java API | Spring Boot 2.5.3、JDK 8（根 `pom.xml`） | 未升级。需在独立 SCA 结果中逐条核对公告受影响范围和实际调用路径。 |
| staging 构建 | `.tools/admin-build.log`：6 个单资源超过 244 KiB，`app` 入口为 1.82 MiB | 记录为非阻断性能告警；未引入 Vue 3/JDK/webpack 迁移。 |

没有把“版本旧”写成“已证实可利用漏洞”。Axios 官方安全页可作为逐条公告核对入口：<https://github.com/axios/axios/security/advisories>。当前没有完整、成功的 SCA 扫描结果，因此漏洞适用性为**待核实**。

## 待验证与提交边界

- 小程序全量发现器已通过：10 个 `*.test.js` 文件，包含新增的工作人员图片下载同源测试。
- 后台 staging 命令已重新生成 `apps/admin/dist`（时间 14:33）；命令捕获只保留到 Webpack 启动输出，未记录可引用的最终退出行，因此不把它计为正式构建验收。
- Java `mvn -pl box-web -am test` 首次下载依赖后留下未产生 Surefire 报告的孤儿 JDK 进程；该进程已按精确 PID 停止。Java 单元测试与隔离 MySQL `verify` 均仍待重新执行，以替换旧的 7 项中 1 失败结果。
- 本次工作目录本来就包含登录、配置、迁移与测试的未提交改动。提交时应仅按文件逐一暂存；禁止 `git add .`，不得包含 `.tools/`、`.env`、数据库、客户资料或归档材料。
- 本记录不执行本地 MySQL 进程收尾，也不宣称已提交或推送。