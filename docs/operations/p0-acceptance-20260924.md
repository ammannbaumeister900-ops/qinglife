# Qinglife P0 统一验收版（2026-09-24）

本轮只有一个验收版本：分支 `release/p0-acceptance-20260924`，固定标签 `acceptance-p0-20260924-r2`。轻友端、工作人员移动工作台、运营后台、Java API 和数据库迁移都来自该版本。后台/API 验收环境与微信预览二维码只是同一版本的两个入口，不再称为不同“候选版”或不同“环境版本”。原标签 `acceptance-p0-20260924` 保留为发现后台导航缺失前的历史快照，不再用于验收。

该版本尚未合并 GitHub `main`。用户完成联合验收后，只将这个固定标签对应的提交合入 `main`；不得再混入 `feat/p0-*`、历史预览包或其他工作树的改动。

验收后台与 API 使用隔离路径、数据库 `qinglife_p0_accept_20260924` 和 Redis DB 1。原有 `/test-admin/`、`/test-api/` 未替换，生产环境未操作。

## 唯一版本与验收入口

| 对象 | 唯一标识或入口 | 说明 |
| --- | --- | --- |
| GitHub 候选分支 | `release/p0-acceptance-20260924` | 验收期间只修验收阻断问题 |
| 固定验收标签 | `acceptance-p0-20260924-r2` | 联合验收和后续合并的唯一源码基准 |
| 运营后台 | https://qinglife.guijianju.cn/p0-admin/ | 使用验收账号及验证码 |
| 业务 API | https://qinglife.guijianju.cn/p0-api/ | 后台令牌或小程序令牌 |
| 轻友端 | 微信小程序 AppID `wxe21e6518a440d613`；二维码 `.tools/acceptance-p0-20260924/p0-preview.png` | 微信真实账号扫码验收 |
| 移动工作台 | 同一小程序「我的」→「工作人员工作台」 | 与轻友端同一个预览包，不是另一套应用 |

## 验收账号

后台验收账号：`p0_admin`、`p0_operator`、`p0_leader`、`p0_finance`。强随机密码保存在本机忽略文件 `.tools/acceptance-p0-20260924/acceptance-accounts.json`，没有写入 Git、远端明文文件或本文档。

移动工作台沿用 `p0_operator` 作为待绑定后台工作人员账号。真实微信账号第一次登录后，在后台「工作人员配置」中核对小程序账号编号，绑定 `p0_operator`，按验收场景勾选「启用工作台」「档案与报名操作」「登记线下收款」。仅凭昵称或手机号不得自动绑定。

2026-09-24 17:22，微信开发者工具已为 AppID `wxe21e6518a440d613` 成功生成预览二维码。预览包包含轻友端和 `/staff/` 工作台分包，其运行代码与后台/API 验收环境来自同一候选源码树；真机微信登录、工作人员账号绑定与真机业务验收仍待执行。

## 已准备的隔离数据

- 期次 `9001`：「P0 验收体验期（测试数据）」，2026-11-01 至 2026-11-03，开放报名，公开报价 199 元，ID `fb3c110d-bc9e-4459-a844-2c308728c75f`。
- 虚构轻友「P0 验收卡次轻友（虚构）」，无手机号，ID `d652afb5-a222-4033-9a61-9e532a0c41f5`。
- 其卡次账户 `a18cae20-018a-4996-8727-1494df32c1e6`，初始 3 次。开户流水已生成；没有实际收款或扣卡。
- 轻读接口可访问；当前没有文章数据，可先验页面空态与隐私边界。

## 建议验收用例

1. **后台登录与菜单**：四个账号均可登录；管理员、运营可进入卡次管理并开户/调整；带领人、财务只能查询卡次。匿名请求卡次接口返回 401，带领人尝试开户或确认结算应返回 403。
2. **期次与报价**：轻友端能看到期次 9001 和 3 个活动日；报价展示为参考金额，不得显示成已结算金额。
3. **报名联系人与参与者**：用真实微信测试号发起两人报名，发起人是主要联系人，两名实际参与者各有独立报名/参与记录；同行手机号留空，不得自动填入发起人手机号。
4. **报名与结算状态**：报名待确认、已确认、已取消与结算待确认、已确认分别展示；轻友端只能看到工作人员确认结果，不能执行现金支付或扣卡。
5. **现金结算**：后台或移动工作台确认现金结算后，最终金额及方式在轻友端显示；期次公开报价仍保持原值，重复确认不得生成重复结算。
6. **多人卡次结算**：给报名发起人开卡，再尝试选择同行人的账户，应拒绝且余额不变；选择发起人的账户成功后，只扣该账户，流水和结算状态同时更新。
7. **单人后台报名扣卡**：单人后台报名选择实际参与者本人的卡次账户；选择其他轻友账户应拒绝。
8. **余额和重试**：余额不足、已确认重入、重复请求都不得再次扣卡或留下部分结算记录；卡次余额不得为负。
9. **卡次展示**：轻友端只显示当前剩余可用卡次和消耗记录；后台可以查询、开卡和按流水调整，不直接覆盖余额。
10. **体验记录隐私**：活动前后私人感受默认只对本人可见；未明确同意分享的内容不得进入轻读或公开分享。
11. **移动工作台授权**：授权前「工作人员工作台」入口不可见；绑定后按 `canOperate`、`canPayment` 分别验证操作；停用授权后入口和 API 访问立即失效。
12. **P1 边界**：不应出现身体情况表、填写入口、后台查询或统计。

## 当前验证

- 公开期次列表曾因 MyBatis `foreach` 注解错误返回 500；本轮修复并新增真实 MySQL 回归测试。修复后公开列表返回 1 个期次、3 个活动日。
- Java 单元测试 47 项、真实 MySQL 集成测试 33 项，失败 0；小程序完整测试通过，其中真实本地 HTTP 生命周期 10 组通过；后台 staging 构建通过。
- 2026-09-24 联合验收发现空库只初始化了 P0 增量菜单，导致后台页面源码存在但入口缺失。已增加不含账号和业务数据的完整业务导航基线迁移，并在新建空库验证用户中心、活动运营、内容发布、轻友动态、其他配置、权限管理及卡次管理均可生成。
- 远端 P0 API、后台入口、四个账号、权限矩阵、公开联系信息和轻读接口均通过 HTTPS 实测；本地 MySQL 测试库在测试后删除。
- GitHub 候选分支与固定验收标签用于锁定本轮唯一源码；验收通过前保持 `main` 不变。
- 本记录不代表真机微信验收、生产部署或线上数据迁移验收。

## 合并门槛

只有以下事项由用户共同验收通过后，才把 `acceptance-p0-20260924-r2` 合入 GitHub `main`：

1. 微信真机可完成登录、两人报名、无手机号同行、活动前后感受和本人历史回看。
2. 工作人员账号绑定、权限区分、停用后失效通过。
3. 现金和卡次结算结果跨后台、移动工作台和轻友端一致；错误归属、余额不足和重复确认均被拒绝。
4. 首页、当期主题/封面、精选轻读和桃子客服入口符合运营预期。
5. 验收过程中没有发现需要改变数据模型或迁移结果的阻断问题。

合并完成仍不等于生产发布。正式旧库接管、生产配置、微信正式域名和生产数据迁移继续按发布流程单独验收。
