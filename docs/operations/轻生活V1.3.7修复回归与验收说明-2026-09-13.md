# 轻生活 V1.3.7 修复回归与验收说明

完成日期：2026-09-13。范围：9月12日主流程、P0、P1报告中的9项缺陷及相关回归。

## 结论

**9项问题已修复并通过对应自动回归，已部署到原隔离测试环境，可交由用户验收。**

- 主流程：通过，新合成轻友完成报名→确认→模拟人工付款→移动到场→结营→回访→后台时间线。
- TC-P0-001～014：14/14通过。
- TC-P1-001～010：10/10通过。
- 附加检查：并发重复收款、多人订单确认与金额边界、完整签到历史、中文查询及空结果、新旧时间线一致性通过。
- 前端：完整 staging 构建成功；5项交互逻辑回归通过（请求失败、响应乱序、筛选重置）。构建仅有资源体积提示。
- 后端：完整 Maven clean package，未跳过测试，构建和部署健康检查通过；业务验收另以真实 Java/MySQL/Redis 接口回归为证据。

2026-09-13用户完成登录后，已在真实浏览器核验签到中文筛选、完整更正历史、报名中文筛选及空结果、第4219978期已确认/已付1元，以及对应完整时间线。上述页面检查通过，全程只读，未变更验收数据。小程序真机逐页操作仍不在这次浏览器核验范围内。本轮不代表9月15日全部产品范围已经完成，也未发布正式环境。

## 请按此顺序验收

入口：https://qinglife.guijianju.cn/test-admin/

使用原测试后台账号登录，无需重新注册或变更权限。用户已完成本轮登录；页面保留在主流程轻友的完整时间线。

| 步骤 | 操作 | 应看到的结果 |
|---|---|---|
| 1. 主链路 | 在报名与付款中按期次 **4219978** 查询；对应轻友为 **V137主流程轻友** | 报名已确认、已付款1元；对应期次已完成 |
| 2. 完整时间线 | 打开上述轻友完整详情 | 建档、报名、确认、付款、到场、结营、回访记录齐全；本轮事件时间集中在 **9月12日21:32:58～21:33:01**，无8小时时差；到场来源为移动工作台 |
| 3. 签到中文搜索 | 活动签到中搜索 **P1签到更正** | 仅返回匹配记录，无500错误 |
| 4. 签到更正留痕 | 点击该记录的 **历史** | 两条变更：未到场→缺席、缺席→已签到；原原因“P1合成缺席原因”和更正原因“P1运营复核更正为到场”均保留，并展示操作人与时间 |
| 5. 报名中文搜索 | 报名与付款中搜索 **P1容量** | 返回2条匹配报名；再搜索不存在的中文关键词，显示空结果 |
| 6. 金额保护 | 查找 **V137单人收款边界**；当前为已确认未付款，应付1元，尝试输入0.5元或2元 | 提示金额必须与应付一致；保持未付款，不增加交易。该记录为合成测试数据，不涉及真实支付 |
| 7. 旧数据展示 | 查看原 **轻生活_赶赶 / 第900001期** 时间线 | 到场显示 **9月12日12:28:32**、来源移动工作台；回访显示 **12:28:33**，顺序正常 |

若希望继续做一次正确付款，可在第6步使用1元模拟登记；这会改变保留验收样例的付款状态，属于测试数据操作。

## 缺陷关闭依据

| 原编号 | 修复 | 结果 |
|---|---|---|
| D-001 / P0-001 | 单人及订单统一校验已确认状态；整单要求所有参与人已确认；事务与行锁保护 | 待确认单人/整单均拒绝，无交易；多人订单部分确认也拒绝 |
| D-002 / P0-005 | 实付必须等于应付，后台与移动工作台遵循相同服务端规则 | 少付、多付被拒绝；足额订单参与人同步为已付款 |
| D-003 / P0-014 | 移动事件明确UTC存储、北京时间展示；旧已核实事件采用来源证据映射 | 新主流程全事件时间一致；原900001期时间显示修复 |
| D-004 | 移动到场正确记录mobile_workspace，扩展数据库来源约束 | 新事件来源正确，重复到场只记一次历史 |
| D-005 | 确认报名补齐首次confirmed_at/confirmed_by；重复确认不覆盖首次值 | 接口与数据库复核通过 |
| D-P1-001 | ASCII轻友编号转为UTF-8再与中文关键字比较；查询失败清空旧列表 | 活动签到中文筛选通过 |
| D-P1-002 | 同步修复报名查询字符集；避免旧响应覆盖新搜索 | 中文匹配、空结果及乱序回归通过 |
| D-P1-003 | 撤销付款、缺席缺少原因返回400业务校验 | 两种场景均返回400、拒绝写入 |
| D-P1-004 | 新增事务内签到历史，记录前后状态、原原因、本次原因、操作者、时间；后台新增历史入口 | 完整两次变更与原因可从接口查询，前端构建通过 |

在线回归另外发现旧防重复提交拦截器使重复确认/收款返回500。已在这些有事务幂等保护的更新接口移除冲突拦截，新增报名接口仍保留原保护。三次并发收款均成功返回，实际仅生成1笔交易与1条付款状态日志。

## 源码、部署与回滚

- 项目根目录：`F:\轻生活项目\轻生活`。
- 修复同步至原 `轻生活打卡小程序/后台代码/life-admin-api`、`life-admin-ui/src`；本地 `deployment/staging/build-src` 同步补齐。
- 新迁移：`轻生活数据库升级V1.1/V1_3_7__attendance_audit.sql`。
- 本轮脚本及结果归档：`deployment/staging/e2e-v137-20260912`。
- 后端最终发布目录：`/opt/qinglife-test/releases/v137-20260912-213105`，保留原jar、源码备份、数据库备份、构建日志和receipt。
- 前端发布备份：`/opt/qinglife-test/releases/20260912-v137/previous-admin`。
- 当前后端jar SHA-256：`166e5b2eccef6c352964a6f61e5c5e5c6d27cc92462cc61e9cef1c2521051be1`。

回滚只针对测试环境：可恢复相应备份中的API包和后台静态资源；新增历史表不应随意删除，以免丢失新增记录。服务器运行包与本地原始源码在修复前逐项核过哈希，源文件同步也校验过未发生额外变更。

## 数据保留说明

第900001期及原主轻友的原始业务记录保持不变。旧到场来源与时间通过独立证据映射修正展示，映射仅匹配已验证的记录ID、原时间、操作人和来源；不按昵称猜测，也不批量重写历史时间。

本次主流程、P1及补充单人边界数据保留供验收。第一次被重复提交拦截器中断的合成批次也保留了失败证据；查验主流程请以 **4219978** 期及下面的ID为准，避免同名误认。所有脚本临时小程序token已在执行结束时删除。

| 主流程对象 | ID |
|---|---|
| 轻友 | 883495b7-0059-4405-ba3b-42942e4900f7 |
| 期次4219978 | 1b9c0f4e-8511-4f52-863c-c509a055f8f1 |
| 报名 | bd1f707d-4fbe-4423-8661-b7e4cb2064d3 |
| 订单批次 | d6c52c05-1dc2-4247-b864-ba3b9fd0c5b9 |
| 到场 | 4cebb6bb-b6a9-4488-bc86-a7990d559cb6 |
| 回访 | 6f6fafda-fc2f-4b92-9773-fdd1a94536f9 |
| 单人金额边界报名 | baaa695b-b0a6-4de9-8cd7-0a7c4b1b8479 |

## 本地证据

同目录 `轻生活V1.3.7验收证据` 包含：

- `p0-regression.json`：14条P0、主流程及附加断言。
- `p1-regression.json`：10条P1及6项附加检查。
- `final-checks.json`：旧时间线展示修复与原始记录保持不变。
- `extra-checks.json`：单人付款边界与整条时间线核验。
- `ui-regression.txt`：5项前端交互逻辑测试结果。
- `receipt.json`：最终API发布回执。

验收后再按保留清单处理合成数据；当前不自动删除用户待验收证据。
