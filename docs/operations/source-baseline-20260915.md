# 轻生活源码基线（2026-09-15）

唯一开发目录：`F:\轻生活项目\轻生活\qinglife`。
基线：GitHub `ammannbaumeister900-ops/qinglife`，提交 `563083560b975392c97eae150d874e5f30eead1b`（2026-09-15 07:11:11 +08:00）。
本地新检出仓库保留原提交历史，整改分支为 `release/readiness-20260915`。未创建或发布新的远端仓库。

## 判定依据

逐文件比较内容，排除 CRLF/LF 换行差异，而非依据目录名或修改日期：
- 小程序：103 个对应文件中 101 个一致，差异是 README 与迁移后的生命周期测试路径。
- 运营后台：318 个对应文件中 317 个一致，仅 README 不同。
- API：492 个对应文件中 486 个一致，差异为 README、合成 Demo 数据及环境配置脱敏。
- 数据库：11 个对应迁移文件全部一致。
- deployment/staging 副本缺少后续服务记录与档案更新。移动改期入口已被新版移除，当前源码明确限制改期状态，不能将旧入口直接合回。
- 详细文件清单见 baseline-comparison-20260915.json。

## 归档

旧代码、Demo、原交付件、旧部署副本与启动脚本已移动至工作区 `历史归档/2026-09-15/`，保留完整目录和私有配置，不提交 Git。
归档内 `移动清单.json` 保存原路径与目标路径，可按清单恢复。没有删除历史数据。
原数据库目录独有的导入工具与契约测试也保存在归档，需校正旧路径并审查后逐项迁入主线，不能直接认定其未入库即为废弃代码。
`builder-os` 是独立个人训练项目，不属于轻生活历史版本，保留原位。用户资产资料、产品文档和 outputs 也保留原位。

## 验证界限

本轮仅对本地与 GitHub 源码进行核对，未登录生产服务器，因此不声称服务器运行包等于本基线。
原报告中的 Java/MySQL/Redis 验收属于历史证据；重新上线前仍须复测。
